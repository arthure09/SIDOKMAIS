# Base URL backend Express. Untuk simulator/web, localhost cukup.
# Untuk device fisik via Expo Go, ganti ke IP Tailscale laptop
# (bukan localhost) — copy file ini ke .env dan isi di sana.
EXPO_PUBLIC_API_URL=http://localhost:3000
